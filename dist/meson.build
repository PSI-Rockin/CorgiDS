icon_themes = ['hicolor']
foreach theme : icon_themes
  install_subdir(theme, install_dir: 'share/icons/')
endforeach

install_data(
  sources : 'com.github.psi_rockin.CorgiDS.desktop',
  install_dir : 'share/applications')

install_data(
  sources : 'com.github.psi_rockin.CorgiDS.appdata.xml',
  install_dir : 'share/metainfo')
